print(" Program started ")

letter = (input("please enter a standard character: "))

if len(letter)==1:
    a_code = ord(letter)
    print (f"AsCII code for {letter} is {a_code}")
else:
          print ("enter only one letter")
print(" program ended ")
